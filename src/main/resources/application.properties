# application.properties replaced by application.yml + profile files.
# Use SPRING_PROFILES_ACTIVE to select environment (dev/test/stage/prod)
# Examples:
# Windows cmd: set SPRING_PROFILES_ACTIVE=dev
# Linux: export SPRING_PROFILES_ACTIVE=prod
# See application.yml and application-{dev,test,stage,prod}.yml for config examples.